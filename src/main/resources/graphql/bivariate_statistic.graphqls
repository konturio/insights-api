type BivariateStatistic {
    axis: [Axis],
    meta: Meta,
    initAxis: InitAxis,
    overlays: [Overlay],
    indicators: [Indicator],
    correlationRates: [PolygonMetrics],
    covarianceRates: [PolygonMetrics],
    colors: Color
}